syntax="proto3";

enum MessageType {
    serverhello = 0;
    clienthello = 1;
    userlist = 3;
    relaymessage = 2;
}

message IRCMessage {
    string dfhkey = 3;
}

message IRCPayload {
    IRCMessage data= 3;
    MessageType mtype;
}