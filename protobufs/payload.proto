syntax="proto3";

enum MessageType {
    clienthello = 0;
    serverhello = 1;
    userlist = 2;
    relaymessage = 3;
}

message IRCMessage {
    string dfhkey = 1;
}

message IRCPayload {
    IRCMessage data= 1;
    MessageType mtype = 2; 
}